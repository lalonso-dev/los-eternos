Pon aquí las fotos que quieras que salgan en la Galería (tab "Fotos").

- Cualquier `.jpg`, `.jpeg`, `.png` o `.webp` que dejes en esta carpeta aparece
  automáticamente en el sitio — no hay que tocar ningún archivo de código.
- Solo copia las fotos aquí y haz `git push`.
